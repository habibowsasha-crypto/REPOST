CHANNEL DM BOT DEVELOPMENT FILES

These files are not required by Railway.

Run the full test suite from the ZIP root:

python -m pytest -q DEV/tests -o asyncio_mode=auto

Production files required by Railway are kept in the ZIP root.

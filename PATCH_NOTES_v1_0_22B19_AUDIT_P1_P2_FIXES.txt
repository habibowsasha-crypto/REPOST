TgBlaster v1.0.22b19 — исправления аудита P1/P2
================================================

База: v1.0.22b18

P1
--
1. Удаление аккаунта:
   - cancel незавершённых dm_pending_queue (status=cancelled, reason=account_deleted);
   - DELETE dm_account_dispatch;
   - сброс preferred/reserved в unified pool;
   - global completed / opt-out / история контактов НЕ трогаются.

2. Timeout + close AsyncOpenAI:
   - AI Быстрый оффер: AI_QUICK_OFFER_TIMEOUT_SECONDS (default 45);
   - Maxim funnel: AI_MAXIM_TIMEOUT_SECONDS (default 45);
   - AI Первый DM: client.close() после wait_for.

P2
--
3. FloodWait больше не затирает pause_reason PeerFlood.
4. Auto-resume: retry mark_spambot_manual_resume если первая попытка упала;
   account уже resumed — dispatcher не зависает на free_detected.
5. AI_DM_DRY_RUN: default в коде остаётся false (совместимость с текущими Railway).
   .env.example предупреждает задавать variable явно; для теста ставьте true.
6. Миграции AI-схемы через PRAGMA table_info + warning вместо except: pass.
7. prune_ai_processed_messages (30 дней / max 200k строк) на startup.

Не изменено
-----------
- first_message.py / VIP Кирилла / kirill_vip_funnel (SHA прежние)
- тексты и веса first DM
- смысл auto-resume (только hardening ошибок)

Новые optional Variables
------------------------
AI_QUICK_OFFER_TIMEOUT_SECONDS=45
AI_MAXIM_TIMEOUT_SECONDS=45
